require('test1',t1)
rel p(Integer x)
	x=1
rel q(String x)
	x='abc'
rel z(fn,x)
	fn(x)
rel main(x)
	Table t = {}
	t2 = table.set(table.set(t,'a',1),'b',2)
	io.writeln(t)
	io.writeln(t2)
	table.get(t2,'a',x)
	io.writeln(x)