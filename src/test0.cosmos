require('test1',t1)
functor(F, Functor)

rel p(Integer x)
	x=1
rel q(String x)
	x='abc'
rel z(fn,x)
	fn(x)

rel main(x)
	Table t = {}
	t2 = table.set(t,1,2)
	io.writeln(t)
	io.writeln(t2)
	table.get(t2,1,x)
	io.writeln(x)
	f = F(1,2,3)
	io.writeln(f)
	f2 = F
	io.writeln(f2)